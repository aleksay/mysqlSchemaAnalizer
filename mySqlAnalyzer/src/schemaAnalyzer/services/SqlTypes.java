package schemaAnalyzer.services;

/**
*
* @author ale
*/
public enum SqlTypes {

   varchar,
   bigint,
   longtext,
   datetime,
   sql_int,
   decimal,
   mediumint, 
   longblob,
   tinytext, 
   tinyint,
   smallint, 
   mediumtext,
   date,
   sql_char, 
   timestamp,
   set,
   sql_enum,
   text, 
   blob,
   time,
}
